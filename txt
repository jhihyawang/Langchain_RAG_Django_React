    def validate_file(self, file):
        if not file.name.lower().endswith(".pdf"):
            raise serializers.ValidationError("只允許上傳 PDF 檔案")
        if file.content_type != "application/pdf":
            raise serializers.ValidationError("檔案類型錯誤，請確認為 PDF")
        return file
